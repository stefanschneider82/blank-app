[project]
name = "blank-app"
version = "0.1.0"
description = "A simple Streamlit app template for you to modify."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.61.1",
]

[tool.uv]
package = false
