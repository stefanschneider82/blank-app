[project]
name = "blank-app-template"
version = "0.1.0"
description = "A simple tournament management app for handball tournaments."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.61.1",
]

[tool.uv]
package = false
